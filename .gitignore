# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# --- Environments ---
.venv/
venv/
env/
.python-version

# --- Tooling caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.hypothesis/

# --- Project outputs (regenerable; never commit) ---
# 临时/私有输出目录一律忽略：每个用户自己跑 `adi-run-all --results-dir <dir>`
# 生成，结果不应进版本库（体积大且强依赖随机种子与依赖版本）。
results/
outputs/
*.png

# ---------------------------------------------------------------------------
# 例外：tools/results/ 是**公开发布的参考输出**，不是临时产物。
# 它被 CI 的 "Determinism" job 当作字节级可复现基准，也是 README 中
# 全部数字与 7 张图的来源。若忽略它，读者 clone 后无法对照论文结论，
# 也无法发现"输出 schema 漂移"这一已知技术债（见 CHANGELOG）。
# 反向规则必须写在 `results/` 与 `*.png` 之后才生效。
# ---------------------------------------------------------------------------
!tools/results/
!tools/results/**
# --- 文档配图始终跟踪 ---
!docs/**/*.png

# ---------------------------------------------------------------------------
# 参考文献一律不进版本库（版权兜底）。
#
# `NOTICE` 列出的论文 [00]、ISSCC 讲稿 [00_1] 与专利 [09]-[14] 是**第三方
# 版权材料**。本仓库只引用条目（作者 / 会议 / DOI / 专利号）并在 NOTICE 中
# 声明 "None of them is redistributed here"；把它们放进公开仓库会与该声明
# 直接矛盾，并构成再分发侵权。
#
# 这条规则是**结构性**的，不依赖人的自觉：即使有人把原文 PDF 解压到工作区，
# `git add -A` 也收不进来。确有需要跟踪的自有文档（例如作者本人的预印本），
# 用 `git add -f <file>` 显式覆盖。
# ---------------------------------------------------------------------------
refs/
references/
literature/
*.pdf
*.ppt
*.pptx
*.key
*.doc
*.docx
*.epub

# --- Editors / OS ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --- Docs build ---
site/
